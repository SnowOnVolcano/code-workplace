//无常量说明，无变量说明

//有返回值函数定义
int function1() {
    //复合语句
    if (-1 * 2 >= 0)
    {
        return (-+1*2);
    }
    return (0);
}
int function2(int Parameter1, int pArameter2, char P) {
    //复合语句
    if ((0 - -1) * 'a' != 1)
    {
        return ('1');
    } else
    {
        return (Parameter1 + pArameter2 * (P + P) * 1  * 'a' * function1() / 1);
    }
    
}



//无返回值函数定义
void function7() {
    //复合语句
    return;
}
void function8(int parameter) {
    //复合语句
    const int max = 10;
    int i = 0;
    while (-1 * i * -1 < 10)
    {
        i = i + 1;
    }
    return;
}

//主函数
void main() {

    int array[10];
    const int ITEM = -1;
    int temp;
    //////////////////condition
    
    for (int _i = function1(); _i == 0; _i = _i - 0)
    {
        _i = -1 + _i;
    }

    //有返回值函数调用语句
    function1();
    function3(ITEM + array[1] * (ITEM + ITEM) * 1 - 0 * -1 * 'a' * function1() / 1);
    
    //无返回值函数调用语句
    function7();
    function8((ITEM + ITEM));
    

    //赋值语句
    temp = array[ITEM * 0];

    //读语句
    scanf(identifier1, identifier2);

    //写语句
    printf("string",0);
    printf(ITEM + ITEM);

    //返回语句
    return;

}